#include <mysql.h>

int create_connect_nodb(char* host, char* user, char* pass) {
    return 0;
}

int create_connect(char* host, char* user, char* pass) {
    return 0;
}

int create_database(int connection, char* statement) {
    return 0;
}

int execute_query(int connection, char* statement) {
    return 0;
}

int execute_read_query(int connection, char* statement) {
    return 0;
}
